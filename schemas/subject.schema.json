{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openuniversity/schemas/subject.schema.json",
  "title": "Subject",
  "description": "Top-level educational domain metadata, e.g. Science, Mathematics.",
  "type": "object",
  "required": ["id", "title", "description"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Lowercase, alphanumeric, unique across the repository."
    },
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "subSubjects": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "description": "IDs of sub-subject directories under this subject."
    }
  },
  "additionalProperties": false
}
